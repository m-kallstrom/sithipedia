<h2>Eligible Candidates for Lordship</h2>

<div class="list">
  <% @eligible_users.each do |master| %>
    <%= link_to "Make #{master.username} a Dark Lord of the Sith?", edit_user_path(master.id) %>
    <br>
  <% end %>
</div>
